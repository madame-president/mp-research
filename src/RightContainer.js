import React from 'react';
import './RightContainer.css';

const RightContainer = () => {
  return (
    <div>
      <h2 className="category-header">Right Category Header</h2>
      {/* Add more content here */}
    </div>
  );
};

export default RightContainer;
